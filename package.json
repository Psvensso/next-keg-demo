{
  "name": "keg-workspace-root",
  "version": "1.0.0",
  "private": "true",
  "description": "",
  "license": "ISC",
  "author": "https://github.com/Psvensso",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "start": "npm run start --workspaces --if-present",
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "db:reset": "npm run reset:prisma --workspace=@keg/db",
    "db:studio": "npm run studio --workspace=@keg/db",
    "db:seed": "npm run seed --workspace=@keg/db"
  },
  "devDependencies": {}
}
